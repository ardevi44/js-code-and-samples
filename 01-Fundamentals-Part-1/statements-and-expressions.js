// Expressions
3 + 4;
1991;
true && false && !false;

// Statement - Not produces a value
if (23 > 10) {
  // Statement - No produces a value it self, instead is an action cause declare a variable
  // But "23 is bigger" is an expression cause is a vlaue it self
  const str = "23 is bigger";
}
